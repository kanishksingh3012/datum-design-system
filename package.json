{
  "name": "atlas-design-system",
  "private": true,
  "version": "0.0.0",
  "license": "MIT",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build:styles": "npm run build --workspace=@atlas-design/styles",
    "build:react": "npm run build --workspace=@atlas-design/react",
    "build": "npm run build:styles && npm run build:react",
    "test": "npm run test --workspace=@atlas-design/react"
  }
}
