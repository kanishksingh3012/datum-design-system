{
  "name": "datum-design-system",
  "private": true,
  "version": "0.0.0",
  "license": "MIT",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build:styles": "npm run build --workspace=@datum-design/styles",
    "build:react": "npm run build --workspace=@datum-design/react",
    "build": "npm run build:styles && npm run build:react",
    "test": "npm run test --workspace=@datum-design/react"
  }
}
